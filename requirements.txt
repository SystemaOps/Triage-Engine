# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# LLM (via OpenRouter — OpenAI-compatible API)
openai>=1.0.0

# RAG / LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# Retrieval (pure Python — no ML deps required)
rank-bm25>=0.2.2
# Database
supabase>=2.0.0

# Utilities
python-dotenv>=1.0.0
